spring:
  application:
    name: ph-shoes-etl

  datasource:
    driver-class-name: net.snowflake.client.jdbc.SnowflakeDriver
    url: "jdbc:snowflake://${SNOWFLAKE_ACCOUNT}.snowflakecomputing.com/?warehouse=${SNOWFLAKE_WAREHOUSE:COMPUTE_WH}&db=${SNOWFLAKE_DATABASE}&schema=${SNOWFLAKE_SCHEMA:PUBLIC}&role=${SNOWFLAKE_ROLE}"
    hikari:
      maximum-pool-size: 10
      minimum-idle: 1
      idle-timeout: 30000
      connection-timeout: 30000
      data-source-properties:
        authenticator: oauth
        token: ${SNOWFLAKE_TOKEN}
        CLIENT_SESSION_KEEP_ALIVE: true

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: net.snowflake.hibernate.dialect.SnowflakeDialect

management:
  endpoints:
    web:
      exposure:
        include: health,info

server:
  port: ${PORT:8080}

logging:
  level:
    root: INFO